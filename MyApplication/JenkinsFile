pipeline {
  agent {
  node{
  label 'master'
  }
  }
  tools {
    maven 'MAVEN_HOME'
  }
  stages{
    stage('build'){
    steps{
        sh 'mvn -version'
        sh 'cd MyApplication'
        sh ' mvn compile package'
        }
      }
    }
  }
